apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-a.fullname" . }}
  labels:
    {{- include "project-a.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: "3000"
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "project-a.selectorLabels" . | nindent 4 }}
